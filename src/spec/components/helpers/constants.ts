// tslint:disable:no-magic-numbers

import { Offset } from '@musical-patterns/utilities'

const DEFAULT_SPEC_BOUND: number = 10000
const OFFSET_TO_EXCLUDE_MIN_OR_MAX: Offset = 0.001

export {
    DEFAULT_SPEC_BOUND,
    OFFSET_TO_EXCLUDE_MIN_OR_MAX,
}
